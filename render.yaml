services:
  # 1. Backend Web Service (Node.js API)
  - type: web
    name: eventpass-api
    runtime: node
    plan: free
    region: oregon
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: eventpass-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d

  # 2. Frontend Static Site (React 18 + Vite)
  # Note: Static sites are served via Render's global CDN, so they cannot have a region.
  - type: static
    name: eventpass-web
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: eventpass-api
          property: host

databases:
  # 3. Managed PostgreSQL Database
  - name: eventpass-db
    plan: free
    region: oregon
    databaseName: shnoor_event_db
    user: shnoor_user
